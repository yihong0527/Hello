%%% Theorem box %%%%%%%%%%%%%%%%%%%%%%%%%%%
\usepackage[framemethod=TikZ]{mdframed}
%\newtheorem{theo}{Theorem}
\newenvironment{theoremboxed}{\begin{mdframed}\begin{theorem}}{\end{theorem}\end{mdframed}}
% Technically it's not seeing this either.
  
\newenvironment{propositionboxed}{\begin{mdframed}\begin{proposition}}{\end{proposition}\end{mdframed}}
\newenvironment{lemmaboxed}{\begin{mdframed}\begin{lemma}}{\end{lemma}\end{mdframed}}
\newenvironment{corollaryboxed}{\begin{mdframed}\begin{corollary}}{\end{corollary}\end{mdframed}}

%\newenvironment{lemmaboxed}{\begin{mdframed}\begin{lemma}}{\end{lemma}\end{mdframed}}
% For some reason it won't see this unless it's in the source file... ugh
% Likewise for propositions and corollary

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%


  
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Example Box
%
%
% http://tex.stackexchange.com/questions/38354/make-an-example-and-shaded-box-like-in-the-book
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%\usepackage[framemethod=TikZ]{mdframed}
\usepackage{xcolor}

% EXAMPLES
%% set the counter for your environment
%\newcounter{example}
\renewcommand{\theexample}{\thesection.\arabic{example}}

%% define the style
\mdfdefinestyle{example}{%
    linecolor=black,
    outerlinewidth=1pt,
    bottomline=true,
    leftline=false,rightline=false,
    skipabove=\baselineskip,
    skipbelow=\baselineskip,
    frametitle=\mbox{},
}
%% setup the environments
%%% with number

% MATT: commented out for now
%\newmdenv[%
%    style=example,
%    settings={\global\refstepcounter{example}},
%    frametitlefont={\bfseries Example~\theexample\quad},
%]{example}

%%% without number (starred version)
\newmdenv[%
    style=example,
    frametitlefont={\bfseries Example~\quad},
]{example*}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
